# Auxiliary function for the vignettes

Ask whether the vignettes have been built using the
`rebuild-long-running-vignette.R` script. This is only useful when using
devtools to release `shapr` to CRAN. See `devtools::release()` for more
information.

## Usage

``` r
release_questions()
```
