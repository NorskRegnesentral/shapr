# Check that needed libraries are installed

This function checks that the `parsnip`, `recipes`, `workflows`, `tune`,
`dials`, `yardstick`, `hardhat` and `rsample`, packages are available.

## Usage

``` r
regression.check_namespaces()
```

## Author

Lars Henry Berge Olsen
